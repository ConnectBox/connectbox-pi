---
# dns-dhcp needs to come last, given it adjusts resolv.conf and breaks
#  subsequent lookups
dependencies:
  - bootstrap
  - dns-dhcp
  - network-interfaces
  - wifi-ap
  - mikegleasonjr.firewall
  - nginx
  - webserver-content
  - usb-content
  - sample-content
