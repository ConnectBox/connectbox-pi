---
# dns-dhcp needs to come last, given it adjusts resolv.conf and breaks
#  subsequent lookups
dependencies:
  - bootstrap
  - dns-dhcp
  - geerlingguy.nginx
  - mikegleasonjr.firewall
  - webserver-content
  - usb-content
  - sample-content
